import 'package:flutter/material.dart';

class AppColors {
  // Background color
  static const Color background = Color(0xFF202020);

  // Main text color
  static const Color mainText = Color(0xFFE2BE7F);

  // Other text (like subtitles, secondary info)
  static const Color otherText = Color(0xFFFFFFFF);

  // Text that matches background (rare, maybe placeholders)
  static const Color backgroundText = Color(0xFF202020);

  static const Color greyColor = Color(0XFF707070);
}
